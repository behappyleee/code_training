<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="play.sample.board.persistence.BoardDAO">
  	
  	<select id="getAllBoardData" resultType="play.sample.board.vo.BoardVO">
		SELECT board_no, board_title, board_writer FROM play_board   	
  	</select>
  
  </mapper>